program test_;

var int a, t, y;

functions
function int fib (int n){
  var int returnValue, x, y;
  if(n <= 1)
  {
    write(1000000000);
    returnValue = n ;
  } else {
    x = fib(n-1);
    y = fib(n-2);
    returnValue = x + y;
    write(9000000000);
    write(returnValue);
  };

  return (returnValue);

}

main {
  a = fib(4);
  write(a);
};
