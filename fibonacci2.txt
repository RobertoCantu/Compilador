program test_;

var int a, t, y;

functions
function int fib (int n){
  var int returnValue, z , w;
  if(n <= 1)
  {
    returnValue = n ;
  } else {
    write(1000000000000);
    write(n);
    returnValue = (fib(n - 1) + (fib(n - 2)));
  };

  return (returnValue);

}

main {
  write(fib(4));
};
