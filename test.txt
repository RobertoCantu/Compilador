program test_;

var int a, b, d;

functions
function int lol(int i){
    var int c;
   
    
    return (i);
}

main {

    d = a;

    for b = 1 to 10 do {
        d = d + 1;
    };

    if (a < 10 && a > 5) {
        write(6);
    };

    a = 8 + lol(4);
};